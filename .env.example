# Copy this file to `.env` in the project root and set your Web client ID.
# Vite does NOT load `.env.example` — only `.env` / `.env.local`.
# Restart the dev server after changing `.env`.
#
# Google Cloud → OAuth Web client → Authorized JavaScript origins, add:
#   • http://localhost:5181 (or your Vite dev port) for the web app
#   • http://127.0.0.1:53463 for the packaged Electron app (fixed local server origin)
# Optional: change Electron port (must match Google Cloud):
# NOTE_APP_OAUTH_PORT=53463
VITE_GOOGLE_CLIENT_ID=
